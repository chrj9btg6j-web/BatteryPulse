# 2026-08-12 更新紀錄

## 發布狀態

- `v2.2.0`：第一個正式穩定版。
- `v2.1.0`：早期測試版，維持 Beta 標示。

## 使用者可見變更

- 更新檢查改為啟動時立即檢查，之後每 12 小時檢查一次。
- 更新提示只在展開後的「總覽」頁顯示「可用更新」與紅點。
- 頂端狀態列不再顯示更新紅點，避免與溫度或警示狀態混淆。
- 點擊總覽的更新提示會開啟 GitHub 更新頁，程式不會在背景自動下載或覆蓋檔案。

## 發布檔案

- `BatteryPulse.TopBar-v2.2.0-20260812.exe`
- `BatteryPulse-Setup-v2.2.0-20260812.exe`
- `SHA256SUMS.txt`

## 驗證範圍

- 確認更新通知來源仍使用 GitHub Releases API。
- 確認無可靠更新資訊時不顯示總覽提示。
- 編譯與安裝包完成後，再進行啟動檢查、更新提示與安裝覆蓋測試。

## 待審核修正項目

- [ ] **GPU 資料必須與實際顯示卡配對**：總覽與頂端狀態列不可再使用未分辨來源的單一 GPU 使用率／溫度欄位。
  - [ ] 內顯使用率只能搭配同一顆內顯的溫度與型號。
  - [ ] 獨顯使用率只能搭配同一顆獨顯的溫度與型號。
  - [ ] 顯示目前實際使用中的顯示卡名稱，例如 AMD Radeon Graphics 或 NVIDIA GeForce RTX 系列。
  - [ ] 內顯與獨顯同時存在時分開列出，不合併成一個模糊的 GPU 數值。
  - [ ] 讀不到對應溫度、使用率或型號時顯示 `--`，不得拿另一張顯示卡的數值代替。
  - [ ] 同步保留資料來源，方便確認是 Windows GPU Engine、LibreHardwareMonitor 或 NVIDIA／AMD 感測介面。
  - [ ] 在 ASUS TUF／ROG、Acer Swift、MSI 測試機上確認配對結果後，才進入可編譯測試版。

## 審核中的總覽修正清單

以下項目已寫入原始碼，但依目前流程尚未編譯、尚未製作新的測試檔，也尚未發布：

- [x] 總覽改為固定三欄三列網格，卡片高度固定，避免數值長短造成版面跳動。
- [x] 將「目前狀態」與「充電瓦數」合併為同一張卡。
- [x] 將「充電預估」改為「充電／消耗預估」；顯示充滿、達充電上限、耗電高於充電、消耗中等狀態。
- [x] 移除總覽的「預估離電使用時間」卡，保留「充電上限」作為首頁可見狀態與控制入口。
- [x] 總覽 CPU／GPU 溫度與使用率維持同性質合併顯示，讀不到的單項以 -- 表示。
- [x] 電池健康頁的容量欄位改為「目前容量」，單位為 Wh；資料優先使用 Windows BatteryStatus 的 RemainingCapacity，沒有時才以電量百分比推算並標註來源。
- [x] 儲存空間改為列出可讀取的 C:、D: 與其他外接磁碟，分別顯示可用百分比與可用／總容量。
- [x] 總覽加入 Acer Center 風格的灰階空心圓圈，分別摘要電池狀態與系統磁碟可用空間。
- [x] 套用方案 A「柔和陰影懸浮」：卡片與充電上限控制項滑入時只增加陰影與淡背景，不下沉、不改變尺寸。
- [x] 充電上限卡固定高度，不再因控制項是否可用而縮短，維持總覽格線對齊。
- [x] 導覽與頁面名稱由「電源與 PD」改為「電源」。
- [x] 在「電源」頁新增耗能排行，列出前五名程序的比例與系統耗電分配估算值；每 5 秒取樣一次，不新增 WMI 或網路查詢。
- [x] 總覽不建立電池健康度卡，電池健康詳細資料只保留在「電池健康」頁。
- [x] 已編譯本輪修改至 dist/review/BatteryPulse.TopBar.overview-review-20260812.exe，等待使用者驗收；尚未製作安裝包。
- [ ] 尚未在多磁碟、外接磁碟與未提供 RemainingCapacity 的電腦上做實機驗收。
- [ ] GPU 內顯／獨顯使用率、溫度、型號的配對修正仍需另外完成並實機核對。

### 預計涉及檔案

- `src\BatteryPulse.cs`
- `src\PerformanceReader.cs`
- `src\AdvancedDashboard.cs`
- `src\TopStatusBarWindow.cs`

## 2026-08-12-2 審核版變更（尚未 commit）

- [x] 總覽電池與儲存空間圓環改為固定畫布與固定欄寬，避免圓環錯位或擠壓文字。
- [x] 「電源」頁移除容易造成誤解的「供電餘裕」與「供電來源」卡，改為電源狀態、充電器類型、電池流向與讀值來源。
- [x] 將電池健康摘要整合到「電源」頁；詳細容量與循環資料仍保留於「電池健康」頁。
- [x] 溫度頁移除電池溫度，新增儲存裝置溫度；僅在 LibreHardwareMonitor／OpenHardwareMonitor 回報時顯示。
- [x] 30 分鐘趨勢新增電池電量（%），圖例補上 W／°C／% 單位，網格線改為可見的淺灰色。
- [x] 頂端狀態列排序改為按住拖曳、灰線導引、放開套用，並顯示成功放置提示。
- [x] 已編譯 review 測試檔：`dist/review/BatteryPulse.TopBar.overview-review-20260812-2.exe`。
- [ ] 等使用者確認畫面與功能後，才建立 commit；本輪未 commit、未上傳。

## 2026-08-12-3 審核版變更（尚未 commit）

- [x] 充電／消耗預估改為依目前狀態切換：充電時顯示至充滿／充電上限的時間，離電時顯示目前儲能可支撐的剩餘使用時間。
- [x] 預估優先使用 Windows 回報的目前容量 Wh，並依最近 10 分鐘的實際電池流向與耗電率做保守估算；缺少目前容量時才退回百分比推算。
- [x] 頂端狀態列新增並預設顯示「預估剩餘時間」項目：充電顯示「充滿／上限」，離電顯示「剩餘」。
- [x] 舊設定檔只遷移一次，將原本預設隱藏的 ETA 顯示出來；使用者之後仍可在設定中關閉。
- [x] 已編譯 review 測試檔：`dist/review/BatteryPulse.TopBar.overview-review-20260812-3.exe`。
- [ ] 等使用者確認畫面與估算結果後，才建立 commit；本輪未 commit、未上傳。
